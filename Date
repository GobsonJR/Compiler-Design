%{
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

char *month[] = {
    "jan", "feb", "mar", "apr", "may", "jun",
    "jul", "aug", "sep", "oct", "nov", "dec"
};
%}

%%
[0-3][0-9]-[0-1][0-9]-[0-9][0-9][0-9][0-9]   {
    char s1[3], s2[3], s3[5];
    int d, m, y;
    s1[0] = yytext[0];
    s1[1] = yytext[1];
    s1[2] = '\0';

    s2[0] = yytext[3];
    s2[1] = yytext[4];
    s2[2] = '\0';

    s3[0] = yytext[6];
    s3[1] = yytext[7];
    s3[2] = yytext[8];
    s3[3] = yytext[9];
    s3[4] = '\0';

    d = atoi(s1);
    m = atoi(s2);
    y = atoi(s3);

    if (m >= 1 && m <= 12)
    {
        printf("%02d %s, %04d", d, month[m - 1], y);
    }
    else
    {
    printf("Invalid date: %s", yytext);
    }
}

.|\n     ECHO;
%%

int yywrap()
{
    return 1;
}

int main()
{
    yyin = fopen("input1.txt", "r");
    if (yyin == NULL)
    {
        printf("File not found\n");
        return 1;
    }

    yylex();
    fclose(yyin);
    return 0;
}
